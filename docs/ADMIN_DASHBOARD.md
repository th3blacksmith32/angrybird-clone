# Admin Dashboard
Open `admin/index.html`, login with the admin password (default admin) to fetch a JWT via `/auth/login`. The dashboard can list users, ban/unban, grant items, and send TON pool balances to a destination wallet using backend `/admin/*` routes.
