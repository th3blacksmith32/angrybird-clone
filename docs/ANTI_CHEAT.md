# Anti-Cheat
`engine/PhysicsValidator.js` provides simple validation comparing launch vectors to reported destruction. Backend `/score/submit` rejects suspicious scores. Fraud middleware ensures billing requests include signed timestamps and unique orderIds.
