# Telegram
Use `webapp/telegram.html` as a lightweight WebApp entry. On button press it simulates a Telegram login by posting telegramId and username to `/auth/telegramLogin` and displays the JWT token.
